{% extends 'base.html.twig' %}
{% block body %}

    <div class="container">
        <div class="form">
            <br><h2> Edit Category </h2><br>
            {{ form_start(form)}}
                {{form_row(form.name, {'attr':{'class': 'w-75 form form-control'}})}}
            
            <input type="submit" value="Save" class="btn btn-primary btn-block mt-3" style=" width: 150px;">
            {{ form_end(form)}}
        </div>
    </div>

{% endblock %}